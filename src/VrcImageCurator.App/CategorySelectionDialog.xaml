<Window x:Class="VrcImageCurator.App.CategorySelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose image category"
        Width="420"
        Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Where should these images go?" />
        <TextBlock Grid.Row="1" Margin="0,4,0,14" Foreground="{StaticResource MutedTextBrush}" Text="Choose the archive category for the selected folder." />
        <ComboBox x:Name="CategoryCombo" Grid.Row="2" VerticalAlignment="Top" automation:AutomationProperties.Name="Destination image category"
                  xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Margin="0,0,8,0" IsCancel="True" />
            <Button Style="{StaticResource PrimaryButton}" Content="Scan folder" IsDefault="True" Click="Confirm" />
        </StackPanel>
    </Grid>
</Window>
